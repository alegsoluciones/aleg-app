import { Box } from 'lucide-react';
import { ModulePlaceholder } from '../../components/ModulePlaceholder';

export const InventoryPage = () => {
    return <ModulePlaceholder title="Gestión de Inventario" icon={Box} color="text-emerald-500" />;
};
